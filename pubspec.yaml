name: latihan2
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.5.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.5.1

  # Icons
  cupertino_icons: ^1.0.8

  # Local Storage (WEEK6: Hive)
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Sensor (WEEK7: Accelerometer)
  sensors_plus: ^4.0.2

  # Camera (WEEK7: PPG-like)
  camera: ^0.10.6

  # Date Formatting (WEEK9)
  intl: ^0.19.0

  # Charts (WEEK9: Dashboard)
  fl_chart: ^0.68.0

  # PDF Generation & Preview (WEEK10-11)
  pdf: ^3.10.9
  printing: ^5.13.1
  path_provider: ^2.1.5
  open_file: ^3.5.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # Hive Code Generation (WEEK6)
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true

